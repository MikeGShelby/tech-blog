<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="text">
      {{comment_text}}
    </div>
    <img src="{{user.user_image}}">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
  </section>
  {{/each}}
</div>